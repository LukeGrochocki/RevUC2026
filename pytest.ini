[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: calls real external APIs (set GARDN_INTEGRATION=1)
# google-genai triggers Python 3.14 DeprecationWarning in google.genai.types (upstream).
filterwarnings =
    ignore::DeprecationWarning:google.genai.types
